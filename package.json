{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "gc-deploy": "gcloud builds submit --tag gcr.io/prov-xyz-dev/my-nuxt-app-name:1.0.0 . && gcloud run deploy --image=gcr.io/prov-xyz-dev/my-nuxt-app-name:1.0.0 --platform managed --port 3000"
  },
  "devDependencies": {
    "firebase-admin": "^11.3.0",
    "firebase-functions": "^4.1.1",
    "firebase-functions-test": "^3.0.0",
    "nuxt": "3.0.0-rc.13",
    "sass": "^1.56.1"
  },
  "dependencies": {
    "firebase": "^9.14.0"
  }
}
