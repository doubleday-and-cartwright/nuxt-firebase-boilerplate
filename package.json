{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "NITRO_PRESET=firebase yarn build && cd .output/server && npm install && cd ../../ && firebase deploy"
  },
  "devDependencies": {
    "firebase-admin": "^11.3.0",
    "firebase-functions": "^4.1.1",
    "firebase-functions-test": "^3.0.0",
    "nuxt": "3.0.0-rc.13"
  },
  "dependencies": {
    "firebase": "^9.14.0"
  }
}
